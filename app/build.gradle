apply plugin: 'com.android.application'

android {
    compileSdkVersion 35
    namespace = "cc.efront.android"

    defaultConfig {
        applicationId = "cc.efront.android"
        minSdkVersion 14
        targetSdkVersion 35
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

}

dependencies {
}
